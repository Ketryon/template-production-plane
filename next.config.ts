import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing clever on purpose. A production plane's value is its boundaries,
  // not its build configuration.
};

export default nextConfig;
