node_modules
.next
.env
.env.local
next-env.d.ts
tsconfig.tsbuildinfo

# Legacy local SQLite store. The call log lives in Postgres now, but an older
# checkout may still have this file — and older versions of it held Fortnox
# tokens, so it must never be committed.
.data
