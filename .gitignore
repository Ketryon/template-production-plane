node_modules
.next
.env
.env.local
next-env.d.ts
tsconfig.tsbuildinfo

# Legacy local SQLite store. The call log lives in Postgres now, but an older
# Never commit a local store — older shapes of this file held vendor tokens.
# tokens, so it must never be committed.
.data
