export default function HomePage() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui" }}>
      <h1>Production plane</h1>
      <p>
        Everything past this point reads production and cannot change it. Add
        your reads as declared entries with a mandatory note — see README.md.
      </p>
    </main>
  );
}
